# AUC_ROC_Project

This project demonstrates **ROC (Receiver Operating Characteristic) curve** and **AUC (Area Under Curve)** using a simple Python program.

## Files
- `roc_auc_demo.py` → Python script that generates sample data, prints threshold → TPR/FPR table, and plots ROC with AUC.

## How to Run
1. Install dependencies:
   ```bash
   pip install scikit-learn matplotlib pandas

